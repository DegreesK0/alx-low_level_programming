#include <stdio.h>

/**
* main - prints out digits 0-9
*
* Return: 0 when successful
*/

int main(void)
{
	int digits = 48;

	for (digits = 48; digits <= 57; digits++)
	{
		putchar(digits);
	}

	putchar('\n');

	return (0);
}
