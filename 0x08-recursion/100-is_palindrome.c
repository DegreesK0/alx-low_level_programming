#include "main.h"
#include <stdio.h>

/**
* is_palindrome - checks if a string is a palindrome
* @s: string to check
*
* Return: retrun 1 if a palindrome, 0 if not
*/

int is_palindrome(char *s)
{
	if (*s == i)
	{
		s++

	}
}

